package com.geoqr.app.network

// Intentionally left empty.
// The DTOs and toDto() extension functions previously declared here (CreateQrDto,
// CreateQrResponseDto, ResolveQrResponseDto, ScanDto) were only used by the now-removed
// duplicate GeoQrRetrofitApi.kt implementation and are no longer referenced anywhere
// in the codebase.
